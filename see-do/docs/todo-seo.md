# Media Naming & Optimization Tasks

- [x] Configure image processor with strict vocabulary filters.
- [x] Structure batch target folders (`local`, `motel`, `TEQ-Queensland`, `theme-parks`).
- [x] Run `node src/media_namer/process_all_images.js` to execute batch compression.
- [x] Verify Google PageSpeed compatibility (all outputs must use modern `.webp` formats).
- [x] Match processed file paths with target pages inside the HTML builder configurations.
